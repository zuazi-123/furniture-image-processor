# 便携版打包说明

## 方式1：包含 Node.js 便携版（推荐）

### 步骤：
1. 下载 Node.js 便携版
   - 下载地址：https://nodejs.org/dist/v20.11.0/node-v20.11.0-win-x64.zip
   - 解压到项目根目录，重命名为 `node-portable`

2. 运行打包脚本
   ```bash
   打包便携版.bat
   ```

3. 打包脚本会自动：
   - 构建项目
   - 复制文件到 furniture-portable
   - 复制 node.exe 到 furniture-portable/app/
   - 安装依赖

4. 压缩 furniture-portable 文件夹
   ```bash
   powershell -Command "Compress-Archive -Path furniture-portable -DestinationPath furniture-portable-v1.1.0.zip -Force"
   ```

## 方式2：不包含 Node.js（用户需自行安装）

### 步骤：
1. 直接运行打包脚本（跳过 Node.js 复制）
   ```bash
   打包便携版.bat
   ```

2. 压缩 furniture-portable 文件夹

3. 用户需要：
   - 安装 Node.js：https://nodejs.org
   - 或将 node.exe 放在 app 目录下

### 当前实现：
- start.bat 已修改为支持两种方式
- 优先使用本地 node.exe
- 如果没有，则使用系统 Node.js
- 都没有则提示用户安装

## 文件大小对比

- **包含 Node.js**：约 200MB
- **不包含 Node.js**：约 180MB

## 推荐方案

**开发/测试环境**：使用方式2（不包含 Node.js）
- 开发者通常已安装 Node.js
- 打包更快，文件更小

**最终发布版**：使用方式1（包含 Node.js）
- 用户无需安装任何依赖
- 真正的"便携版"
- 解压即用

## 当前打包状态

当前打包文件：`furniture-portable-v1.1.0-final.zip` (182MB)
- ❌ 不包含 node.exe
- ✅ 支持使用系统 Node.js
- ✅ 包含 UMI-OCR
- ✅ 包含所有依赖

如需包含 node.exe，请按方式1重新打包。
