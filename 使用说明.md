# 百度OCR代理服务器使用说明

## 快速开始

### 1. 启动代理服务器

双击运行 `启动代理服务器.bat` 文件，或在命令行中执行：

```bash
node proxy-server.js
```

看到以下提示说明启动成功：
```
========================================
  百度OCR代理服务器已启动！
  监听端口: 3000
  代理地址: http://localhost:3000
========================================
```

**重要：保持这个窗口打开！**

### 2. 配置百度OCR

1. 访问 https://console.bce.baidu.com/ai/#/ai/ocr/overview/index
2. 注册/登录百度智能云账号
3. 创建应用，选择"通用文字识别"
4. 获取 API Key 和 Secret Key
5. 在工具界面顶部展开"百度OCR配置"，填入这两个Key

### 3. 使用工具

1. 上传家具图片
2. 设置网格行列数
3. 点击"切图+自动识别"按钮
4. 等待识别完成（会使用百度OCR高精度识别）
5. 编辑识别结果
6. 导出Excel

## 注意事项

- 每次使用前必须先启动代理服务器
- 代理服务器窗口不能关闭
- 百度OCR每天有500次免费额度
- API Key和Secret Key会保存在浏览器中，下次无需重新填写

## 故障排除

### 问题1：提示"百度OCR调用失败"
- 检查代理服务器是否启动
- 检查API Key和Secret Key是否正确
- 查看代理服务器窗口是否有错误信息

### 问题2：识别效果不好
- 确保图片清晰
- 调整网格行列数，确保每个格子只包含一件家具
- 识别后可以手动修改错误的结果

### 问题3：代理服务器启动失败
- 确保已安装Node.js
- 检查3000端口是否被占用
- 尝试以管理员身份运行

## 技术支持

如有问题，请检查：
1. Node.js是否已安装（运行 `node -v` 检查）
2. 代理服务器是否正常运行
3. 浏览器控制台是否有错误信息
