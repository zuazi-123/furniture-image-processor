# Electron 桌面应用打包说明

## 📦 打包步骤

### 1. 安装依赖（如果还没安装）

```bash
npm install
```

### 2. 打包成 Windows 可执行文件

```bash
npm run dist
```

打包完成后，在 `release` 文件夹中会生成：
- `家具图片处理工具 Setup 1.0.0.exe` - 安装程序
- `家具图片处理工具 1.0.0.exe` - 便携版（可选）

### 3. 分发给用户

将 `家具图片处理工具 Setup 1.0.0.exe` 发送给用户，用户双击安装即可使用。

## 🚀 开发模式

如果你想在开发时测试 Electron 应用：

```bash
npm run electron:dev
```

这会同时启动 Vite 开发服务器和 Electron 窗口。

## 📝 注意事项

### 图标文件

如果你想自定义应用图标，需要准备：
- `icon.ico` - Windows 图标（256x256）
- `icon.png` - Linux 图标（512x512）
- `icon.icns` - Mac 图标

可以使用在线工具将 PNG 转换为 ICO/ICNS：
- https://www.icoconverter.com/
- https://cloudconvert.com/

### 打包大小

首次打包会下载 Electron 运行时（约 150MB），打包后的安装程序大约 80-100MB。

### 打包选项

- `npm run pack` - 只打包不生成安装程序（用于测试）
- `npm run dist` - 打包并生成安装程序（推荐）

## 🎯 用户使用流程

1. 用户下载 `家具图片处理工具 Setup 1.0.0.exe`
2. 双击安装
3. 安装完成后，桌面会有快捷方式
4. 双击快捷方式，自动启动应用
5. 代理服务器会自动在后台运行
6. 关闭窗口时，代理服务器也会自动关闭

## 🔧 常见问题

### 问题1：打包失败

确保已安装所有依赖：
```bash
npm install
```

### 问题2：打包很慢

首次打包需要下载 Electron，请耐心等待。后续打包会快很多。

### 问题3：杀毒软件报警

这是正常现象，因为 Electron 应用会被一些杀毒软件误报。可以添加信任或使用代码签名证书。

### 问题4：想打包 Mac 版本

在 Mac 电脑上运行：
```bash
npm run dist
```

会生成 `.dmg` 文件。

## 📊 打包后的文件结构

```
release/
├── 家具图片处理工具 Setup 1.0.0.exe  # 安装程序（分发这个）
├── win-unpacked/                      # 解压后的文件（不需要分发）
└── builder-debug.yml                  # 调试信息（不需要分发）
```

## 🎉 完成

打包完成后，只需要将 `家具图片处理工具 Setup 1.0.0.exe` 发送给用户即可！

用户体验：
- ✅ 双击安装，无需配置环境
- ✅ 自动启动代理服务器
- ✅ 像普通软件一样使用
- ✅ 不需要看到任何代码
