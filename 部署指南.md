# 在线部署指南

## 🌐 部署到 Vercel（推荐，完全免费）

### 步骤 1：注册 Vercel 账号

1. 访问 [Vercel](https://vercel.com)
2. 点击 "Sign Up" 注册
3. 选择 "Continue with GitHub" 使用 GitHub 账号登录

### 步骤 2：导入项目

1. 登录后，点击 "Add New..." → "Project"
2. 选择 "Import Git Repository"
3. 找到你的仓库 `furniture-image-processor`
4. 点击 "Import"

### 步骤 3：配置项目

1. **Project Name**: 保持默认或自定义
2. **Framework Preset**: 选择 "Vite"
3. **Build Command**: `npm run build`
4. **Output Directory**: `dist`
5. 点击 "Deploy"

### 步骤 4：等待部署完成

- 部署通常需要 1-2 分钟
- 完成后会显示你的网站地址，例如：`https://furniture-image-processor.vercel.app`

### 步骤 5：访问你的网站

- 点击提供的链接即可访问
- 首次访问需要输入密码

---

## 🔐 密码管理

### 默认密码

当前设置的密码（在 `Login.vue` 文件中）：
- `furniture2024`
- `admin123`

### 修改密码

1. 打开 `Login.vue` 文件
2. 找到 `validPasswords` 数组（约第 23 行）
3. 修改或添加密码：

```javascript
const validPasswords = [
  'your_password_1',  // 你的密码1
  'your_password_2',  // 你的密码2
  // 可以添加更多密码
]
```

4. 提交并推送更改：

```bash
git add .
git commit -m "更新密码"
git push
```

5. Vercel 会自动重新部署（约1分钟）

### 密码特性

- ✅ 登录后保持 7 天
- ✅ 支持多个密码
- ✅ 密码存储在本地浏览器
- ✅ 可随时退出登录

---

## 🚀 其他部署选项

### Netlify（免费）

1. 访问 [Netlify](https://www.netlify.com)
2. 使用 GitHub 登录
3. 点击 "Add new site" → "Import an existing project"
4. 选择你的 GitHub 仓库
5. 配置：
   - Build command: `npm run build`
   - Publish directory: `dist`
6. 点击 "Deploy site"

### Cloudflare Pages（免费）

1. 访问 [Cloudflare Pages](https://pages.cloudflare.com)
2. 连接 GitHub 账号
3. 选择仓库
4. 配置：
   - Build command: `npm run build`
   - Build output directory: `dist`
5. 点击 "Save and Deploy"

---

## 📱 分享给他人

部署完成后，你会得到一个网址，例如：
- `https://furniture-image-processor.vercel.app`

### 使用方式

1. **分享网址**：把网址发给需要使用的人
2. **提供密码**：告诉他们访问密码
3. **权限控制**：
   - 想让某人访问：告诉他密码
   - 不想让某人访问：不告诉他密码，或者修改密码

### 修改密码后

- 所有人需要重新登录
- 旧密码立即失效
- 实现权限控制

---

## 🔧 本地测试

在部署前，可以本地测试密码功能：

```bash
npm run dev
```

访问 `http://localhost:5173`，会看到登录页面。

---

## ⚠️ 注意事项

1. **密码安全**：
   - 密码存储在代码中，不要使用重要密码
   - 这是简单的访问控制，不是企业级安全方案
   - 适合小团队或个人使用

2. **百度 API**：
   - 在线版本需要配置 CORS 代理
   - 或者使用客户端模式（用户自己填写 API Key）

3. **自动部署**：
   - 每次 `git push` 后，Vercel 会自动重新部署
   - 无需手动操作

---

## 📞 获取帮助

如有问题，可以：
1. 查看 Vercel 部署日志
2. 检查浏览器控制台错误
3. 确认 Node.js 版本兼容（建议 16+）

---

## 🎉 完成！

现在你的工具可以通过网址访问了，随时随地都能使用！
