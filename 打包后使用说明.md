# 打包后使用说明

## 问题说明
打包后点击识别时提示"百度OCR请求限制"，是因为代理服务器没有启动。

## 解决方法

### 方法1：使用批处理文件（推荐）
1. 双击运行 `启动代理服务器.bat`
2. 保持命令行窗口打开
3. 打开打包后的应用
4. 点击"开始识别"按钮

### 方法2：手动启动
1. 打开命令行（cmd）
2. 进入项目目录：
   ```
   cd "c:\Users\Lwz\Desktop\家具图\试试"
   ```
3. 运行代理服务器：
   ```
   node proxy-server.js
   ```
4. 保持命令行窗口打开
5. 打开打包后的应用

### 方法3：使用npm命令
```bash
npm run proxy
```

## 注意事项
- 代理服务器必须保持运行状态
- 如果关闭命令行窗口，需要重新启动
- 确保端口3000没有被其他程序占用

## 开发环境
开发环境（npm run dev）不需要手动启动代理服务器，Vite会自动代理。

