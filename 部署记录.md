# Render 部署 OCR API 服务记录

## 项目背景
家具图片处理工具需要部署一个 OCR API 服务到 Render，作为百度 OCR 的备用方案。

## 遇到的问题及解决过程

### 问题 1：无法推送到 GitHub
**现象**：执行 `git push` 时连接失败
**解决**：多次重试，网络恢复后成功推送

### 问题 2：Render 找不到 main.py
**现象**：
```
python: can't open file '/app/umi-ocr/main.py': [Errno 2] No such file or directory
```

**原因**：
- 最初尝试部署 UMI-OCR（一个桌面应用），不适合作为 Web 服务
- Render 配置中 Dockerfile 路径指向了错误的目录

**解决方案**：
1. 放弃 UMI-OCR，改用 RapidOCR（更适合 Web 部署）
2. 创建轻量级的 Flask API 服务
3. 将文件放到 Render 配置指向的 `umi-ocr-docker/` 目录

### 问题 3：Render 配置路径无法修改
**现象**：Render 控制台中 Dockerfile Path 设置为 `umi-ocr-docker/`，无法编辑

**解决方案**：
- 不修改 Render 配置
- 直接将正确的文件复制到 `umi-ocr-docker/` 目录
- 让 Render 使用该目录下的 Dockerfile

### 问题 4：依赖包版本冲突
**现象**：
```
ERROR: Cannot install -r requirements.txt (line 5) and pillow==10.1.0
because these package versions have conflicting dependencies.
The conflict is caused by:
    The user requested pillow==10.1.0
    rapidocr-onnxruntime 1.3.11 depends on Pillow<=10.0.1
```

**解决方案**：
- 将 `pillow==10.1.0` 降级到 `pillow==10.0.1`
- 满足 rapidocr-onnxruntime 的依赖要求

## 最终方案

### 技术栈
- **OCR 引擎**：RapidOCR (rapidocr-onnxruntime)
- **Web 框架**：Flask 3.0.0
- **跨域支持**：Flask-CORS 4.0.0
- **图像处理**：Pillow 10.0.1, OpenCV, NumPy
- **部署平台**：Render (Docker)

### 文件结构
```
umi-ocr-docker/
├── Dockerfile          # Docker 配置
├── requirements.txt    # Python 依赖
├── ocr-api.py         # Flask API 服务
└── README.md          # 说明文档
```

### Dockerfile 配置
```dockerfile
FROM python:3.9-slim
WORKDIR /app

# 安装系统依赖
RUN apt-get update && apt-get install -y \
    libgl1 libglib2.0-0 libgomp1 libsm6 libxext6 libxrender-dev \
    && rm -rf /var/lib/apt/lists/*

# 安装 Python 依赖
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# 复制应用代码
COPY ocr-api.py .

# 暴露端口
EXPOSE 10000

# 启动服务
CMD python ocr-api.py
```

### API 端点

#### 1. 健康检查
- **URL**: `GET /`
- **响应**:
```json
{
  "status": "ok",
  "message": "OCR API Service is running",
  "version": "1.0.0"
}
```

#### 2. OCR 识别
- **URL**: `POST /ocr`
- **请求方式 1**：文件上传
  - Content-Type: `multipart/form-data`
  - 参数: `image` (文件)

- **请求方式 2**：Base64 编码
  - Content-Type: `application/json`
  - Body:
  ```json
  {
    "image_base64": "base64编码的图片数据"
  }
  ```

- **响应**:
```json
{
  "success": true,
  "text": "识别的完整文本",
  "words": [
    {
      "text": "单词1",
      "confidence": 0.95
    }
  ]
}
```

### 部署配置

#### Render 设置
- **Dockerfile Path**: `umi-ocr-docker/ ./Dockerfile`
- **Docker Build Context**: `umi-ocr-docker/`
- **Docker Command**: (空，使用 Dockerfile 中的 CMD)
- **Port**: 10000
- **Auto-Deploy**: On Commit

#### 环境变量
- `PORT=10000` (Render 自动设置)
- `HOST=0.0.0.0`

## 部署成功

### 服务地址
- **主域名**: https://furniture-image-processor.onrender.com
- **健康检查**: https://furniture-image-processor.onrender.com/
- **OCR API**: https://furniture-image-processor.onrender.com/ocr

### 部署时间
- 2026年1月24日 01:37 AM

### 部署状态
✅ 服务已上线并正常运行

## 经验总结

### 成功要点
1. **选择合适的技术方案**：RapidOCR 比 UMI-OCR 更适合 Web 部署
2. **适配平台配置**：将文件放到 Render 配置指向的目录
3. **解决依赖冲突**：仔细检查依赖包版本要求
4. **保持耐心**：网络问题需要多次重试

### 注意事项
1. Render 免费版有以下限制：
   - 15分钟无请求会自动休眠
   - 首次唤醒需要 30-60 秒
   - 每月有使用时长限制

2. RapidOCR 特点：
   - 完全免费，无调用限制
   - 识别速度较快
   - 支持中文和英文
   - 适合轻量级应用

3. 后续优化建议：
   - 添加请求队列处理并发
   - 实现缓存机制提高性能
   - 添加请求日志和监控
   - 考虑升级到付费版避免休眠

## Git 提交记录

```bash
# 主要提交
be40d6d - feat: 添加基于 RapidOCR 的轻量级 OCR API 服务
af81d98 - fix: 更新 umi-ocr-docker 目录为 RapidOCR API 服务
bfd6243 - fix: 修复 Pillow 版本冲突，降级到 10.0.1
```

## 下一步计划

1. **前端集成**：
   - 在前端项目中添加 RapidOCR API 调用
   - 实现百度 OCR 和 RapidOCR 的切换逻辑
   - 添加错误处理和重试机制

2. **性能优化**：
   - 测试识别准确率
   - 优化图片预处理
   - 添加批量识别支持

3. **监控和维护**：
   - 设置 Uptime 监控避免休眠
   - 收集使用数据和错误日志
   - 根据实际使用情况调整配置

---

**记录时间**: 2026年1月24日
**记录人**: Claude Code Assistant
