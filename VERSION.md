# 家具图片识别工具 - 版本更新日志

## v1.1.0 (2026-02-02)

### 新增功能
- ✅ **导入Excel功能**：支持导入之前导出的Excel文件，继续编辑工作
  - 自动恢复所有家具数据（名称、数量、价格）
  - 自动恢复嵌入的图片
  - 按分类恢复数据
  - 显示导入进度和详细日志

### 功能优化
- ✅ **所有导出功能添加Loading提示**
  - 导出Excel：显示"正在生成Excel文件..."
  - 下载图片：显示"正在准备图片..." → "正在打包图片..."
  - 导出分色版：显示详细的处理进度

- ✅ **纯图版添加拖拽上传功能**
  - 支持拖拽图片到上传区域
  - 拖拽时显示高亮效果
  - 支持多张图片同时拖入

- ✅ **移除自动检测功能**
  - 删除了不稳定的自动边框检测功能
  - 保留手动绘制框的方式，更可靠

### 界面调整
- 导入Excel按钮移到顶部，与"批量选择家具图片"按钮并列
- 优化按钮颜色：导入按钮使用绿色，更醒目

### 技术改进
- 改进Excel图片提取逻辑，使用`worksheet.getImages()`方法
- 修复变量名冲突问题
- 添加详细的调试日志，便于问题排查

---

## v1.0.0 (2026-01-27)

### 初始版本
- 图文版：批量识别家具图片，导出Excel表格
- 纯图版：拖拽分类，导出分色版
- 支持多种导出格式
- 便携版打包，无需安装

---

## 使用说明

### 导入Excel继续编辑
1. 点击顶部"📥 导入Excel继续编辑"按钮
2. 选择之前导出的Excel文件
3. 系统自动恢复所有数据
4. 继续编辑或添加新项

### 注意事项
- 导入会清空当前所有数据，请确保已保存
- 只能导入由本系统导出的Excel文件
- 图片会被完整恢复（如果Excel中包含图片）

---

## 打包文件
- `furniture-portable-v1.1.0-final.zip` (207MB)
  - ✅ **包含完整的应用程序**
  - ✅ **包含 Node.js 运行环境（node.exe）**
  - ✅ **包含 UMI-OCR 识别引擎**
  - ✅ **真正的便携版，解压即用**
  - 双击 `start.bat` 或 `一键启动.bat` 即可使用
  - **已修复：浏览器自动打开功能**
  - **新增：纯图版拖拽上传功能**

### 使用说明
- **无需安装任何依赖**
- 解压后直接运行即可
- 启动脚本会优先使用内置的 node.exe
- 如果内置 node.exe 不可用，会尝试使用系统 Node.js
